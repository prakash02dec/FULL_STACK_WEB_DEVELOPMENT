when you assign position to absolute then it get stacked according to html flow by default..
we can change its position by using z-index: ;
z-index works on all position type execpt static and none .. and by default all positon have z-index 0